import React, { Fragment } from 'react';

import '../src/scss/main.scss';

const Main = ({ Component }) => {
	console.log(``);

	return (
		<Fragment>
			<Component />
		</Fragment>
	);
};

export default Main;
